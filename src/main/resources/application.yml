spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/jinhan;
    username: jinhan
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      #      ddl-auto: none
      ddl-auto: create
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true

logging:
  level:
    org.hibernate.sql: debug
    org.hibernate.type: trace
